{
  "name": "chatserver",
  "version": "1.0.0",
  "description": "This is a chatserver that support RESTful API request and response with SocketIO",
  "main": "index.js",
  "scripts": {
    "start": "node ./api/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "ChatServer"
  ],
  "author": "Andrew Chan",
  "license": "ISC",
  "dependencies": {
    "socket.io": "^4.1.3"
  }
}
